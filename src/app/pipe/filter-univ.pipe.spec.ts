import { FilterUnivPipe } from './filter-univ.pipe';

describe('FilterUnivPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUnivPipe();
    expect(pipe).toBeTruthy();
  });
});
