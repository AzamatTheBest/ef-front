import { FilterFoundPipe } from './filter-found.pipe';

describe('FilterFoundPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFoundPipe();
    expect(pipe).toBeTruthy();
  });
});
